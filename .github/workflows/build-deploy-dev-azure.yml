# name: Build and Deploy PDEX in DEV Azure

# on:
#   push:
#     branches:
#       - dev
# jobs:
#   # Print variables for logging and debugging purposes
#   checkEnv:
#     name: Check Env variables
#     runs-on: ubuntu-latest
#     steps:
#     - name: Print Env Vars
#       run: |
#         echo Git Base Ref: ${{ github.base_ref }}
#         echo Git Build ID: ${{ github.event.number }}
#         echo Git Pull Request Ref: ${{ github.event.pull_request.head.sha }}
#         echo Git Ref Name: ${{ github.ref_name }}
#   # Build PDEX
#   build:
#     name: Build project
#     runs-on: ubuntu-latest
#     concurrency:
#       group: ${{ github.ref }}
#       cancel-in-progress: true
#     env:
#       BUILD_ID: ${{ github.event.number }}
#       ACR_NAME: acrpdexdev
#       AKS_NAME: aks-pdex-dev
#       AZ_RG: rg-pdex-dev-central-ca
#       KEY_VAULT_NAME: kv-pdex-dev
#     permissions:
#       id-token: write
#       contents: read

#     steps:
    
#       # Checkout the PR branch
#       - name: Print env
#         run: |
#           echo BUILD ID: $BUILD_ID
#           echo BUILD NAMESPACE: $BUILD_NAMESPACE
#           echo BRANCH: $BUILD_REF
#           echo ACR NAME: $ACR_NAME
#           echo AKS NAME: $AKS_NAME
#           echo AZURE RG: $AZ_RG

#       - name: Checkout Target Branch
#         uses: actions/checkout@v4

#       - name: Azure Login
#         uses: azure/login@v1
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

#       - name: Build and Push to ACR
#         run: |
#           # Login to ACR using the federated credential
#           az acr login --name ${{ env.ACR_NAME }}
          
#           # Verify login succeeded
#           az acr repository list --name ${{ env.ACR_NAME }} --output table || echo "ACR login verified"
          
#           # Build image
#           docker build -t ${{ env.ACR_NAME }}.azurecr.io/pdex:${{ github.sha }} .
          
#           # Push image with explicit error handling
#           docker push ${{ env.ACR_NAME }}.azurecr.io/pdex:${{ github.sha }}

#       - name: Deploy to AKS
#         run: |
#           az aks get-credentials --resource-group ${{ env.AZ_RG }} --name ${{ env.AKS_NAME }}
          
#           # Ensure Vault Agent Injector is installed
#           if ! kubectl get namespace vault-system &> /dev/null; then
#             echo "Installing Vault Agent Injector..."
#             kubectl create namespace vault-system
#             helm repo add hashicorp https://helm.releases.hashicorp.com
#             helm repo update
#             helm install vault hashicorp/vault \
#               --set "global.externalVaultAddr=https://vault.developer.gov.bc.ca" \
#               --set "injector.enabled=true" \
#               --set "injector.externalVaultAddr=https://vault.developer.gov.bc.ca" \
#               --set "server.enabled=false" \
#               --namespace vault-system
            
#             # Wait for injector to be ready
#             kubectl wait --for=condition=ready pod -l app.kubernetes.io/name=vault-agent-injector -n vault-system --timeout=120s
#           else
#             echo "Vault Agent Injector already installed"
#           fi
          
#           # Update the image tag in the deployment file
#           sed -i "s|acrpdexdev.azurecr.io/pdex:latest-dev|${{ env.ACR_NAME }}.azurecr.io/pdex:${{ github.sha }}|g" azure/k8s-dev-deployment.yaml
          
#           # Apply the deployment
#           kubectl apply -f azure/k8s-dev-deployment.yaml
          
#           # Wait for rollout to complete
#           kubectl rollout status deployment/pdex-app-dev -n default --timeout=5m

#       - name: Finished OC Deploy from yaml
#         run: |
#           echo "FINISHED OC Deploy DEV PDEX"
#           echo ++++++++++
